{% extends 'base.html.twig' %}

{% block body %}
        <div class="container">
    <div class="text-center">
        
<h1 class="text-center"><p class="bg-primary">
                    Dodaj zlecenie    
                </p>
               
        </h1>
        </div> 
        <br>
        {{ form_start(form) }}
            {{ form_widget(form) }}
                <div class="text-center">
                    <input type="submit" class="btn-success" value="Utwórz zlecenie" />
                </div>    
        {{ form_end(form) }}
    </div>
    <ul>
        <li>
            <a href="{{ path('commission_index') }}"><button type="button" class="btn btn-warning">Powrót do listy</button></a>
        </li>
        <li>
        {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('box_user_admin') }}"><button type="button" color="white" class="btn btn-danger">
                PANEL ADMINA
            </button></a>
                {% endif %}
        </li>
    </ul>
{% endblock %}
